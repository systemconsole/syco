
input {
	file{
		 path => "/var/log/messages"
    		type => "syslog"  # a type to identify those logs (will need this later)
	}

}

input {
	stdin {}

}





output {

	stdout {}
}
output {
  	rabbitmq {
    		exchange => "apps"
    		host => "127.0.0.1"
    		exchange_type => "fanout"
		user => "logstash"
		password => "logstash"
		port => 5671
		ssl => true
		verify_ssl => true
  }
}

