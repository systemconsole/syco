<VirtualHost *:80>
    DirectoryIndex fareway.php
    DocumentRoot /var/www/html/fareoffice-fareway/
    ServerName fareway.fareoffice.com
# Change to TC/AV.    ServerAlias tc-fareway-production.fareoffice.com
   
    #Enable mod_rewrite engine
    RewriteEngine On

    #RewriteRule ^test$ http://%{SERVER_NAME}/fareway_test_client_form.php [R=301,NC]
    Include /etc/httpd/conf.d/mod-rewrite/fareway-rewrites.conf

    ErrorLog /var/log/fareway_error_log
    CustomLog /var/log/fareway_acess.log common
    #RewriteLog /var/log/fareway_rewrite.log
    #RewriteLogLevel 3

    #Enable gzip compression
    <Location />
      # Insert filter
      SetOutputFilter DEFLATE

      # Netscape 4.x has some problems...
      BrowserMatch ^Mozilla/4 gzip-only-text/html

      # Netscape 4.06-4.08 have some more problems
      BrowserMatch ^Mozilla/4\.0[678] no-gzip

      # MSIE masquerades as Netscape, but it is fine
      # BrowserMatch \bMSIE !no-gzip !gzip-only-text/html

      # NOTE: Due to a bug in mod_setenvif up to Apache 2.0.48
      # the above regex won't work. You can use the following
      # workaround to get the desired effect:
      BrowserMatch \bMSI[E] !no-gzip !gzip-only-text/html

      # Don't compress images
      SetEnvIfNoCase Request_URI \
      \.(?:gif|jpe?g|png)$ no-gzip dont-vary

      # Make sure proxies don't deliver the wrong content
      Header append Vary User-Agent env=!dont-vary
    </Location>
</VirtualHost>

<VirtualHost *:443>
    DirectoryIndex fareway.php
    DocumentRoot /var/www/html/fareoffice-fareway/
    ServerName fareway.fareoffice.com
# Change to TC/AV.     ServerAlias tc-fareway-production.fareoffice.com

    SSLEngine on
    SSLCertificateFile /etc/httpd/ssl/wild.fareoffice.com.crt 
    SSLCertificateKeyFile /etc/httpd/ssl/wild.fareoffice.com.key

    #Enable mod_rewrite engine
    RewriteEngine On

    #RewriteRule ^test$ http://%{SERVER_NAME}/fareway_test_client_form.php [R=301,NC]
    Include /etc/httpd/conf.d/mod-rewrite/fareway-rewrites.conf

    ErrorLog /var/log/fareway_error_log
    CustomLog /var/log/fareway_acess.log common

    #Enable gzip compression
    <Location />
      # Insert filter
      SetOutputFilter DEFLATE

      # Netscape 4.x has some problems...
      BrowserMatch ^Mozilla/4 gzip-only-text/html
    
      # Netscape 4.06-4.08 have some more problems
      BrowserMatch ^Mozilla/4\.0[678] no-gzip

      # MSIE masquerades as Netscape, but it is fine
      # BrowserMatch \bMSIE !no-gzip !gzip-only-text/html

      # NOTE: Due to a bug in mod_setenvif up to Apache 2.0.48
      # the above regex won't work. You can use the following
      # workaround to get the desired effect:
      BrowserMatch \bMSI[E] !no-gzip !gzip-only-text/html

      # Don't compress images
      SetEnvIfNoCase Request_URI \
      \.(?:gif|jpe?g|png)$ no-gzip dont-vary

      # Make sure proxies don't deliver the wrong content
      Header append Vary User-Agent env=!dont-vary
    </Location>
</VirtualHost>

