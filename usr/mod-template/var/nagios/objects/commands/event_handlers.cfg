define command {
    command_name    handle_switch_port_status_fail
    command_line    if [ $1 = "CRITICAL" ] then rm -f /tmp/syco-switch-port-status fi
    }

define command {
    command_name    handle_switch_mac_status_fail
    command_line    if [ $1 = "CRITICAL" ] then rm -f /tmp/syco-switch-macs fi    
    }

define command {
    command_name    handle_iptables_line_count_fail
    command_line    if [ $1 = "CRITICAL" ] then rm -f /tmp/syco-firewall-line-count fi
    }


