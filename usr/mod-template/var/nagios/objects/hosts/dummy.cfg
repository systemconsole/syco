define host{
    host_name  dummy
    alias dummy 
    address 0.0.0.0
    use host
    hostgroups host, guest, firewall, switch, http-server, mysql-server, dns-server, glassfish-server, ldap-server, backup-server, firewall, log-server, mail-server
    notifications_enabled 0
    register 0
}