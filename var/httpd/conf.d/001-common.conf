# TODO: Remove this?
# SETTING FOR WITCH GLASSFISH DOMAIN TO USE
# SetEnv production one
# SetEnv candidate two

# Setting up so that we can use multi domains on same ip.
# Can we use this on prod server? Don't we need to have the same
# cert configured on all domains to get this to work?
# NameVirtualHost *:80
# NameVirtualHost *:443
